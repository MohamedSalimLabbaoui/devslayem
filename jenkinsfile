pipeline {
    agent any

    tools {
        maven 'M2_HOME'  // Remplacer par votre nom Maven
        jdk 'JAVA_HOME'          // Remplacer par votre nom JDK
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                url: 'https://github.com/MohamedSalimLabbaoui/devslayem.git'
            }
        }

        stage('Build') {
            steps {
                echo "🔨 Build du projet..."
                sh 'mvn clean install -DskipTests'
            }
        }
    }

    post {
        success {
            emailext(
                to: 'labbaouisalim749@gmail.com',
                subject: "✔ SUCCESS - ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: "Le build a réussi. Voir: ${env.BUILD_URL}"
            )
        }
        failure {
            emailext(
                to: 'labbaouisalim749@gmail.com',
                subject: "❌ FAILED - ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: "Le build a échoué. Voir: ${env.BUILD_URL}"
            )
        }
    }
}